apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: tracksapi-cluster
spec:
  instances: 1
  storage:
    size: 1Gi
    storageClass: smb-sc
  bootstrap:
    initdb:
      database: tracksapi
      owner: app
 

