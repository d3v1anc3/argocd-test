# Argo CD Example Setup

When you have setup your Kubernetes cluster and deployed argo-cd on it, run the following command to load the configuration in Argo CD.

```bash
kubectl apply -f bootstrap/bootstrap.yaml
```

